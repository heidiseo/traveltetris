<div class="container col-12">

  <div class="no-results">
    <% unless @warning.blank? %>
      <p>We are sorry, no flights were found.</p>
    <% end %>
  </div>


  <div class="flight-info">


      <% @going_flights.each do |date, flights| %>

<!--
    <div class="cards justify-content-center"> -->
       <div class="cards justify-content-center">
            <div class="card-columns d-flex align-items-center">
           <div class="card grey">
              <% dep_time_format = Date.parse(date) %>
              <% arr_time_format = flights.first.arrival_date %>

             <div class="card-body text-centre">
               <p class="card-text"><%= dep_time = dep_time_format.strftime("%d")%> <%= dep_time_format.strftime("%B")%> <%= dep_time_format.strftime("%Y")%> - </p>
               <p class="card-text"><%= arr_time = arr_time_format.strftime("%d")%> <%= arr_time_format.strftime("%B")%> <%= arr_time_format.strftime("%Y")%></p>
             </div>
           </div>
         </div>
         <div class="card-columns d-flex align-items-center">
          <% flights.each do |flight| %>
            <div class="card">
              <div class="card-body">
                <p class="card-text"><%= flight.arrival_city.name  %></p>
                <p class="card-text">£ <%= flight.price_cents %></p>
              </div>
            </div>
          <% end %>
        </div>

    </div>

     <!--  </div> -->

    <% end %>

    </div>
  </div>
  <%= link_to "Book",  new_booking_path, class:"btn-treehouse"%>
</div>

